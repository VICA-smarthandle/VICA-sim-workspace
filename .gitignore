# colcon build artifacts
build/
install/
log/

# python bytecode
__pycache__/
*.pyc

# local tooling state
.omx/

# Rendered runs. animate_run writes gif/mp4/png here, a few MB per lane and
# regenerated from the result JSONs whenever they are wanted, so the videos
# are output rather than source.
media/

# editor / OS
*.swp
*~
.DS_Store

# Claude Code local (per-machine) settings
.claude/settings.local.json
